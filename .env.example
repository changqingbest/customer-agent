# Default mode is mock so the full project runs without any API key.
LLM_MODE=mock

# Optional remote LLM settings. Only needed when LLM_MODE=remote.
LLM_BASE_URL=https://api.openai.com/v1/chat/completions
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

TOP_K=4
MIN_RELEVANCE_SCORE=0.18
